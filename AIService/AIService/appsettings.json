{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "RabbitMQ": {
    "HostName": "localhost",
    "Port": 5672,
    "UserName": "guest",
    "Password": "guest",
    "QueueName": "ai-processing-queue",
    "Durable": true,
    "AutoAck": false
  },
  "AI": {
    "ApiKey": "your-api-key-here",
    "Model": "gpt-4",
    "BaseUrl": "https://api.openai.com/v1/",
    "SystemPrompt": "You are a helpful assistant. Extract information from the user's message and return it as a JSON array of objects. Each object should contain relevant fields based on the message content."
  }
}
